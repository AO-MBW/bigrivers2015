@using Bigrivers.Server.Model
@{
    List<ButtonItem> listButtonItems = ViewBag.listButtonItems;
}
<div id="wrapper">
    @ViewBag.errormsg
    <h1>@ViewBag.Title</h1>
    <div id="actionbar">
        <div class="action-block">
            @Html.ActionLink("Item Toevoegen", "New", "ButtonItems")
        </div>
    </div>
    <div id="content">
        <table>
            <thead>
                <tr>
                    <th>Weergavenaam</th>
                    <th>URL</th>
                    <th>Status</th>
                    <th>Acties</th>
                </tr>
            </thead>
            <tbody>
                @foreach (var m in listButtonItems.Where(m => m.Status))
                {
                    <tr>
                        <td>@m.DisplayName</td>
                        <td>@m.URL</td>
                        <td>@Html.ActionLink("Actief", "SwitchStatus", "ButtonItems", new { m.Id }, new { @onclick = "return confirm('Als je dit menuitem uitschakelt, is het niet meer zichtbaar op de frontpage en wordt het uit de volgorde gehaalt. Weet je dit zeker?');" })</td>
                        <td>
                            @Html.ActionLink("Wijzig", "Edit", "ButtonItems", new { m.Id }, null)
                            @Html.ActionLink("Verwijder", "Delete", "ButtonItems", new { m.Id }, new { @onclick = "return confirm('Je staat op het punt om " + m.DisplayName + " te verwijderen. Weet je dit zeker?');" })
                            @Html.ActionLink("Up", "OrderUp", "ButtonItems", new { m.Id }, null)
                            @Html.ActionLink("Down", "OrderDown", "ButtonItems", new { m.Id }, null)
                        </td>
                    </tr>
                }
                @foreach (var m in listButtonItems.Where(m => !m.Status))
                {
                    <tr class="inactive">
                        <td>@m.DisplayName</td>
                        <td>@m.URL</td>
                        <td>@Html.ActionLink("Inactief", "SwitchStatus", "ButtonItems", new { m.Id }, null)</td>
                        <td>
                            @Html.ActionLink("Wijzig", "Edit", "ButtonItems", new { m.Id }, null)
                            @Html.ActionLink("Verwijder", "Delete", "ButtonItems", new { m.Id }, new { @onclick = "return confirm('Je staat op het punt om " + m.DisplayName + " te verwijderen. Weet je dit zeker?');" })
                        </td>
                    </tr>
                }
            </tbody>
        </table>
    </div>
</div>
