
@{
    ViewBag.Title = "Artiesten";
    List<Artist> artistList = ViewBag.ArtistsList; 
}

<h1 class="head-h1">Artiesten</h1>
<div class="artists">
    @foreach (Artist artist in artistList)
    {
        <div class="col-xs-3">
            @Html.ActionLink(artist.Name, "Artist", new { Id = artist.Id })
        </div>
    }
</div>

