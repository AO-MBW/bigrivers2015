@{
    ViewBag.Title = "Artiesten";
    List<Artist> artistList = Model; 
}

<div class="content">
    <h1 class="page-title">Artiesten</h1>
        @foreach (var artist in artistList)
        {
            <h3>@Html.ActionLink(artist.Name, "Artists", new { Id = artist.Id })</h3>
        }
</div>